{
  "name": "vite-template-solid",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "clean": "rm -rf ./node_modules ./dist ./yarn.lock .yarn-error.log",
    "dev": "esbuild index.js --outfile=dist_dev/app.js --bundle --sourcemap --watch",
    "serve": "servor ./dist/ index.html --static",
    "dev:file": "node ./scripts/dev.mjs",
    "serve1": "node ./scripts/serve.mjs",
    "dev:cli": "mkdir -p ./www && cp ./public/* ./www/* && esbuild --bundle ./src/index.tsx --outdir=./www --servedir=www && rm -rf ./www"
  },
  "license": "MIT",
  "devDependencies": {
    "@craftamap/esbuild-plugin-html":"0.4.0",
    "esbuild": "^0.14.47",
    "esbuild-plugin-compress": "^0.3.0",
    "esbuild-plugin-solid": "^0.4.2",
    "esbuild-visualizer": "^0.3.1",
    "servor": "^4.0.2"
  },
  "dependencies": {
    "@suid/icons-material": "^0.2.4",
    "@suid/material": "^0.4.2",
    "solid-js": "^1.4.2"
  }
}
